<form method="post" action="{{ path('weapon_user_delete', {'id': weapon_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weapon_user.id) }}">
    <button class="btn">Delete</button>
</form>
