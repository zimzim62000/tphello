{% extends 'base.html.twig' %}

{% block title %}ItemType{% endblock %}

{% block body %}
    <h1>ItemType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ item_type.name }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ item_type.weight }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('item_type_index') }}">back to list</a>

    <a href="{{ path('item_type_edit', {'id': item_type.id}) }}">edit</a>

    {{ include('item_type/_delete_form.html.twig') }}
{% endblock %}
