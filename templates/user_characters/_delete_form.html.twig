<form method="post" action="{{ path('user_characters_delete', {'id': user_character.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_character.id) }}">
    <button class="btn">Delete</button>
</form>
