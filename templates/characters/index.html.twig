{% extends 'base.html.twig' %}

{% block title %}Characters index{% endblock %}

{% block body %}
    <h1>Characters index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
                <th>Image</th>
            </tr>
        </thead>
        <tbody>
        {% for character in characters %}
            <tr>
                <td>{{ character.id }}</td>
                <td>{{ character.name }}</td>
                <td>
                    <a href="{{ path('characters_show', {'id': character.id}) }}">show</a>
                    <a href="{{ path('characters_edit', {'id': character.id}) }}">edit</a>
                </td>
                <td>
                    <img width="200px" src="{{ asset('uploads/images/' ~ character.image)}}"/>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('characters_new') }}">Create new</a>
{% endblock %}
